import { Provider, CountryRestriction } from '@/types';

export async function fetchProviders(): Promise<Provider[]> {
  return [];
}

export async function fetchRestrictions(): Promise<CountryRestriction[]> {
  return [];
}
